import type { Meta, StoryObj } from '@storybook/react'

import SpendPoCLayout from './SpendPoCLayout'

const meta: Meta<typeof SpendPoCLayout> = {
  component: SpendPoCLayout,
}

export default meta

type Story = StoryObj<typeof SpendPoCLayout>

export const Primary: Story = {}
